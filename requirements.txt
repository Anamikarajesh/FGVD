# Core data / ML
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.7
joblib>=1.3
networkx>=3.0

# PyTorch (install matching CUDA wheel from pytorch.org if needed)
torch>=2.5,<3.0
torchvision

# PyTorch-Geometric core. Companion wheels (torch-scatter, torch-sparse,
# torch-cluster, torch-spline-conv) must match your torch+CUDA version
# and are installed separately - see README "Setup" section.
torch-geometric>=2.6

# Notebook
jupyter
ipykernel
